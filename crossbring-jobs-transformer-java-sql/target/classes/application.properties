spring.application.name=jobs-transformer

# Kafka
bootstrap.servers=${BOOTSTRAP_SERVERS:localhost:9092}
topics.jobs=${TOPIC_JOBS:public.jobs}
topics.jobDetails=${TOPIC_JOB_DETAILS:public.job_details}
topics.out=${TOPIC_OUT:jobmodel.stg_jobs}

# Postgres (used by downstream sink processes if added later)
datasource.url=${JOBMODEL_DSN:jdbc:postgresql://localhost:5432/postgres}
datasource.user=${JOBMODEL_USER:postgres}
datasource.password=${JOBMODEL_PASSWORD:password}

